import './Header.css';

export default function Header(){
  return (
    <header>
      <h1>CSS linear scale generator</h1>
      <p>Generate a css calc function to linearly scale a value between 2 viewport widths</p>
    </header>
  )
}